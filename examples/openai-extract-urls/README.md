# OpenAI Extract URLs Example

Demonstrates using the `@sisu-ai/tool-extract-urls` tool with the OpenAI adapter.

Run with:

```bash
npm run dev -w examples/openai-extract-urls -- "Find links in this text"
```
