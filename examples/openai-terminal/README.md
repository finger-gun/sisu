## openai-terminal

Demonstrates the sandboxed terminal tool (`@sisu-ai/tool-terminal`) with OpenAI tool-calling.

Run:

```bash
MODEL=gpt-4o-mini npm run dev -w examples/openai-terminal -- --trace
```

