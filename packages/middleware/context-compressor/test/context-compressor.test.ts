import { test, expect } from 'vitest';

test('context-compressor smoke test', () => {
	// This package currently has no implementation; ensure test runner finds at least one test.
	expect(true).toBe(true);
});
