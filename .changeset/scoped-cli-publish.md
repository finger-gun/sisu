---
"@sisu-ai/cli": minor
"@sisu-ai/skill-install": minor
---

Rename the publishable CLI package names to scoped npm packages.

- `sisu` becomes `@sisu-ai/cli`
- `sisu-skill-install` becomes `@sisu-ai/skill-install`

The executable names stay the same:

- `sisu`
- `sisu-skill-install`
